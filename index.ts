import "/helpers/arrays.ts";
import "/helpers/sets.ts";
import { isDigit, isInt, isNumber } from "/helpers/numbers.ts";
import { Board } from "/helpers/board.ts";

let input = Deno.readTextFileSync("input.txt");
